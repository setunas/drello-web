{
  "extends": [
    "next/core-web-vitals",
    // `prettier` should be put last in the array,
    // so it gets the chance to override other configs.
    "prettier"
  ],
  "rules": {
    "@typescript-eslint/no-unused-vars": "error"
  }
}
